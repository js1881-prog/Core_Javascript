const BitSet = (function() { // BitSet을 반환
    function isValid(set, n) {
        
    }

    function has(set, byte, bit) {

    }

    const BTIS = new Uint8Array([1, 2, 4, 8, 16
    , 32 ,64 ,128]);

    const MASKS = new Uint8Array([~1, ~2, ~4, ~8
    ,~16, ~32, ~64, ~128]);

    return class BitSet {

    };
    
}());


console.log(BitSet);